<div class="card card--margin card__align">
    <img src="{{$image}}" class="card-img-top img--150_250" alt="portada comic">
    <div class="card-body">
        <!--Nota personal con formato de barra de progreso-->
        <div class="progress-bar">
            {{$progress}}
        </div>
        <h4 class="text--Komika text--center">{{$title}}</h4>
        <p class="card-text">{{$content}}</p>
    </div>
  </div>
